= Глоссарий: Логические операторы в Python

== Логические операторы

В Python есть три основных логических оператора, которые позволяют создавать сложные условия:

- `and` — логическое умножение
- `or` — логическое сложение
- `not` — логическое отрицание

== Таблица истинности оператора and

#table(
  columns: 3,
  inset: 8pt,
  align: center,
  table.header([a], [b], [a and b]),
  [False], [False], [False],
  [False], [True], [False],
  [True], [False], [False],
  [True], [True], [True],
)

== Таблица истинности оператора or

#table(
  columns: 3,
  table.header([a], [b], [a or b]),
  [False], [False], [False],
  [False], [True], [True],
  [True], [False], [True],
  [True], [True], [True],
)

== Таблица истинности оператора not

#table(
  columns: 2,
  inset: 10pt,
  align: center,
  table.header([a], [not a]),
  [False], [True],
  [True], [False],
)

== Приоритет выполнения логических операторов

1. В первую очередь выполняется **логическое отрицание** `not`
2. Далее выполняется **логическое умножение** `and`
3. Затем выполняется **логическое сложение** `or`

Для явного указания порядка выполнения условий используют **скобки**:
